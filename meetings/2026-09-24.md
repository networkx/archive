# NetworkX Developer Discussions: September 24, 2026

[Previous meeting notes](https://github.com/networkx/archive/tree/main/meetings) are available. 
Please add News, Discussion and Topics below.
Meeting link: https://colgate.zoom.us/j/92619161786

## Meeting Info
- Meeting time Thursdays 12-1 ET (5pm UTC)
- [Publicly available analytics for our docs pages](https://views.scientific-python.org/networkx.org)

**In attendance**: Dan, Ross

## Topics 
- NX v3.7 Released!! :tada:
  * No complaints yet :crossed_fingers: 

- We switched to CircleCI medium gen2 to save resources. 

- Looks like doc-deployment isn't working (at least this morning): [error msg](https://github.com/networkx/networkx/actions/runs/36020817953/job/107704935645)  Has anything changed with permissions/locations recently?
    - restarting (a couple of times -- Thanks Ross!) worked!

- skills file: 
  - search for use of read weighted edgelist github wide.
    - https://github.com/search?q=nx.read_weighted_edgelist&type=code
    - people are creating skills files for networkx
    - they seem to be doing it wrong (should just say: don't use this -- use read_edgelist instead)

- maintenance skills
  - https://github.com/pypa/pyproject-metadata/pull/301

- pygraphviz minor release 2.0.2 (3.15 wheels)
  - https://pypi.org/project/pygraphviz/2.0.2/

- chordality and self-loops
  * Another little inconsistency with self-loops `chordal_graph_cliques`
  * https://github.com/networkx/networkx/pull/8913

- New viz kwargs: https://github.com/networkx/networkx/pull/8907
  * Two different proposals in 1 PR:
    1. Add a `rotation` kwarg to allow node labels to be rotated
    2. Allow horizontal/vertical alignment kwargs to accept dicts
  * rossbar +1 on both ideas - the accompanying gallery example
    does a good job of motivating both changes

- Wider discussion about viz kwargs:
  * Adding support for passing in viz args as dicts mapping
    to nodes/edges solves one of the major issues CJ
    brought up - i.e. having to call draw functions multiple
    times for different node/edge viz
  * How close are we to doing so already within `draw` itself?

- More CI improvement proposals - use uv to cut down installation times
- * https://github.com/networkx/networkx/pull/8928

- Group betweenness
  - Still to do: expand the tests from the recent changes to the other `group_*_centrality` functions
    - `prominent_group`
      - used logic copied from betweenness centrality, so this has similar errors to what that had, e.g. for normalized and endpoint both True. 
      - currently gives results that don't match group_bC
      - the code needs refactoring
        - converts to pandas and numpy to do dict-easy stuff
        - uses recursion but saves all needed data in a NetworkX graph called DF_tree. Better to leave out recursion and use use a queue
        - should use `sum(<comprehension>)`, not for-loop to sum.
    - `group closeness` and others look much easier.

- Cycle search [PR 8791](https://github.com/networkx/networkx/pull/8791)
  * new stuff seems interesting, hopefully boothby can take a look!

- Would be good to have a quick-easy way to check the many properties of graphs: connected, selfloops, etc.
  - caching?
  - which specific properties?
  - are any of these cheap to update while building the graph?

- Maybe we should create an errata page -- but certainly a note in the docstrings of algorithms where we gave answers and they were wrong and now are fixed.
  - similar idea: keep notices of how to change code when we remove features/functions. Often these notices are inside deprecation warnings

- `sphinx-gallery` added support for parallel execution.  Merged!  :tada:

- Draw: new keyword features (per node label rotation) [#8907](https://github.com/networkx/networkx/pull/8907)
  - new plotting example focused on node labels
  - additional code to support rotation and h/v_alignment

- [Kleitman-wang PR 8710](https://github.com/networkx/networkx/issues/8710) This looks ready to merge -- but are there unfinished decisions to be made?
  - Let's wait til after release to have time to explore differences in sampling effectiveness
        
- Optimal edit paths update https://github.com/networkx/networkx/pull/8590
  * Spent some time with the MRE and noticed some (potentially) interesting behavior with cost specifications
  - Might there be an issue with default costs when only a subset are specified?
    
- Expanded testing
  * A few patterns keep coming up that we don't really have
    a mechanism for, e.g.
    * Seed sweeps for random graphs (cf)
    * separate NX repo for "extended testing" 
        * backends might have trouble running them both

- We have algorithms modules `dominating.py` and `dominance.py`. What would be involved with combining the two? They are both 10+ years old. The main namespace access doesn't change but access by way of the module name does. Do we go to the effort of adding get_attr entries to the old modules? 
  - Maybe this is a 4.0 issue -- I think we said we will deal with namespace stuff then.

- InfoMap: [PR 8727](https://github.com/networkx/networkx/pull/8727)
  * community detection based on random walks which inform path encoding length as a measure of entropy(?).
  * Infomap landing page: https://mapequation.org/infomap/
  * tests now written against hardcoded values. Then a separate test checks all hardcoded values against the InfoMap in C++.
  * lingo differs (e.g. community <-> module, quality <-> map equation, redone to introduce use terms from NX first and point to terms from infomap literature)

- Agent Skills Examples
  * https://github.com/pypa/pyproject-metadata/pull/301
  * https://github.com/henryiii/skills
  * https://www.youtube.com/watch?v=UNzCG3lw6O0
  
- HITS: power iteration vs. spectral decomposition: one, the other, both?
  * https://github.com/networkx/networkx/pull/8620
  * lets do both... with a "meth" kwarg.
- Modularity functions
  - Lot's of new ones!
  - How to organize/advertise? How to use with community detection functions?

- Node "expansion" function (I.e. replace node with a subgraph) See [use-case.](https://github.com/networkx/networkx/pull/8596) Conceptually the "opposite" of node contraction
- Additional geometric graph generators
  * Most other libraries (wolfram, sage, etc.) have explicit generators for e.g. Archimedean solids. In principle, we do too with the `LCF_graph` generator! Not very discoverable though... can/should we do something about this?
      * gallery example, 
      * table of named graphs in LCF_graph docstring,
      * add functions that call LCF_graph under the hood
- `_build_paths_from_predecessors`: https://github.com/networkx/networkx/pull/8460
  * PR merged - still interested in seeing if it can't be improved using
    dict as LIFO!
  * I(dan) build a dict as LIFO example for the new tarjan strongly connected component PR. (That PR had good timing scripts and a 3-tuple queue that was faster to breaking into 3 separate list-based queues. The hard thing to do is "peek" at the top. It requires popping and re-adding. Still O(1) operation. I think it could work well for cases where we have (node, iter()) tuples in the queue. The 3-tuple case reduced to 2-tuple and a separate dict and the dict as LIFO was just barely slower than the 3 separate list-queues. So it might be faster compared to 2-tuples in a list queue.
  * Let's add a dict as LIFO example to NetworkX somewhere... maybe easiest in the recent Tarjan. Maybe in the depth-first-search code
- panther algorithms [PR 8578](https://github.com/networkx/networkx/pull/8578)
    - report from edge case testing and energy from author have put forward fairly widespread changes. Backward incompatible changes due to off-by-one path length is perhaps biggest.
    - These are approximate methods. Hard to know how to test or check/ensure backward compatibility -- and whether that is as important in this seeting.
    - Could introduce a kwarg to keep old definition for a deprecation period. But the old answer is wrong. So probably should just break things.
- Voronoi [PR 8585](https://github.com/networkx/networkx/pull/8585)
- Trying out `display` in gallery
  * Draft PR incoming...
  * Running notes while working through: https://hackmd.io/@eBSTfWl9QO-pActuKqTJyA/ryml8rhEbg
  * Some items might be good while draft pr is coming:
      * change pos to allow dict
- optimal edit paths [PR 8590](https://github.com/networkx/networkx/pull/8590)
  * C3->P3 example is instructive (should probably be added to docstring)
  * Is there a bug?
    * First step: what would constitute a bug?
      - For rossbar:
        * High priority bug: if missing lower-cost solutions
        * Lower priority bug: if missing other (non symmetrical)
          edit paths with the same cost
    * Next step: Can we find instances of either of these?
      - Note also the "labeled vs. unlabeled" results may be
        throwing users off: is there really a bug, or a mismatch
        of user expectations given the default subst/ins/del costs?
- bug in dominating set and very old draft PR [1573](https://github.com/networkx/networkx/pull/1573) that probably fixes it and more.
- floating point examples [8389](https://github.com/networkx/networkx/pull/8389)
    - many comments in the issue, but not many PRs I think the gsoc students didn't end up doing that much. Am I missing something?
- random_walk followup
  * generator in, remaining TODOs:
    - [x] Improve docstring, esp. with examples of different termination conditions
    - [ ] benchmarks
    - [ ] ? make generate_random_paths private?
    - [x] Multiple implementations
      * [8525](https://github.com/networkx/networkx/pull/8525) - proposed impl during review
      * [8528](https://github.com/networkx/networkx/pull/8528) - nested generators
- Minimum feedback edge set : https://github.com/networkx/networkx/pull/8521
  * New proposal, seems like a reasonable add
  
## Todo
- Probably should Update docs to point to iplotx and/or display as needed. (Action: open an issue)
- We should help out contributors who have a single function optimization for their use-case. Probably by making backends with one function easy to use/share/morph.
- Linting 2: Enforce `pytest.raises` as context manager?
  * Ruff rule for this: https://docs.astral.sh/ruff/rules/legacy-form-pytest-raises/

## Old Topics
- Benchmarking: Building off what Derek put together.
    - pytest.benchmark vs asv for benchmarks? Are there better tools. 
        - What did Derek use in the end? Are there tools within that we could build on.
    - Three aspects to benchmarking:
        - presentation of results
        - running and data storage (ascii/csv/json, etc)
        - authoring (which datasets as well as parameters of the functions)
    - Can we put together a set of functions and datasets?
        - Create a NetworkX suite of graph benchmarks

# Topics for the next summit
- read-only dicts version of views
- free-threading 
- parallel free-threading/parallel discussions
- work with scipy.sparse.csgraph to find a way to use accelerated code with NetworkX.